[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Маркеры
markers =
    slow: тесты с реальными NLP-моделями (запускать отдельно: pytest -m slow)
    integration: интеграционные тесты

# Вывод
addopts =
    --tb=short
    --strict-markers
    -q